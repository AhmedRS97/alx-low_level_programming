#include "main.h"

/**
 * print_sign - print the sign of integer n
 * @n: an integer
 *
 * Return: Always int.
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if (n < 0)
	{
		_putchar('-');
		return (-1);
	}
	else
	{
		_putchar('0');
		return (0);
	}
}

